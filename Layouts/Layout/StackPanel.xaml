<Window x:Class="Layouts.Layout.StackPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Layouts.Layout"
        mc:Ignorable="d"
        Title="StackPanel" MinHeight="200" MaxHeight="200" MinWidth="300" MaxWidth="300">
    
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Width" Value="100"/>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Width" Value="100"/>
        </Style>
    </Window.Resources>
    
    <StackPanel Margin="20 20 20 20">
        <StackPanel Orientation="Horizontal" Margin="5 5 5 5">
            <TextBlock Text="Feld 1"/>
            <TextBox/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="5 5 5 5">
            <TextBlock Text="Feld 2"/>
            <TextBox/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="5 5 5 5">
            <TextBlock Text="Feld 3"/>
            <TextBox/>
        </StackPanel>
        
    </StackPanel>
</Window>
